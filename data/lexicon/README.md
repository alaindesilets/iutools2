# `data/lexicon/` -- lexicographic data

Empty for now. Intended home for dictionaries:

- The Spalding dictionary (parsed from source, currently parsed by
  `tools/parse_spalding_dictionary.py` and embedded in `:composeApp`).
- The morpheme dictionary.

See [`../README.md`](../README.md).
