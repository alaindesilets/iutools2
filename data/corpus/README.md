# `data/corpus/` -- corpus-derived compilations

Empty for now. Intended home for data aggregated from text corpora:

- Hansard word-frequency lists and the Benoit-decomposition cache
  (currently `data/grammar/fst/hansard-cache/`, to move here).
- Translation memory (bilingual sentence pairs).
- The morpheme-frequency prior and similar corpus statistics.

See [`../README.md`](../README.md).
